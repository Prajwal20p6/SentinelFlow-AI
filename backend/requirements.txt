# SentinelFlow AI — Backend Dependencies
# ── Core Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9

# ── Database ──
sqlalchemy>=2.0.30
alembic>=1.13.0
aiosqlite>=0.20.0
asyncpg>=0.29.0


# ── Vector Database ──
qdrant-client>=1.10.0

# ── Authentication & Security ──
pyjwt[crypto]>=2.8.0
passlib[bcrypt]>=1.7.4
pyotp>=2.9.0
cryptography>=42.0.0
enkryptai-sdk>=1.0.0

# ── ML & Data ──
scikit-learn>=1.5.0
numpy>=1.26.0

# ── HTTP & Integration ──
httpx>=0.27.0
requests>=2.32.0
websockets>=12.0

# ── Observability ──
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
prometheus-client>=0.20.0
structlog>=24.2.0

# ── Redis (optional, falls back to in-memory) ──
redis>=5.0.0
fakeredis>=2.23.0

# ── Testing ──
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
gunicorn>=22.0.0

