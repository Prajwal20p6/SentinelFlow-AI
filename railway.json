{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m venv backend/venv && backend/venv/bin/pip install -r backend/requirements.txt && cd frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "bash startup.sh",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
